{
  "configurations": [
    {
      "name": "Python: Current File",
      "request": "launch",
      "program": "${file}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}